server:
  port: 8080

spring:
  flyway:
    enabled: false
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://43.134.96.44:3306/agent_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: dbadmin
    password: Db@Adm123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

agent:
  cors:
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:}
  jwt:
    user-secret: ${JWT_USER_SECRET:user-secret-key-at-least-256-bits-long-for-hs256}
    user-expiration: 604800000
    admin-secret: ${JWT_ADMIN_SECRET:admin-secret-key-at-least-256-bits-long-for-hs256}
    admin-expiration: 7200000
  llm:
    provider: mock
    api-key: ${LLM_API_KEY:your-api-key}
    base-url: ${LLM_BASE_URL:https://api.minimax.chat}
  mcp:
    default-url: http://localhost:9000
  knowledge:
    path: ./knowledge
